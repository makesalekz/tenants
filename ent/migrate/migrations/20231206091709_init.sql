-- Create index "members_identity_id_key" to table: "members"
CREATE UNIQUE INDEX "members_identity_id_key" ON "members" ("identity_id");
-- Create "groups" table
CREATE TABLE "groups" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "deleted_at" timestamptz NULL, "identity_id" uuid NOT NULL, "name" character varying NOT NULL, "description" character varying NOT NULL, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "tenant_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "groups_tenants_groups" FOREIGN KEY ("tenant_id") REFERENCES "tenants" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "group_tenant_id_name" to table: "groups"
CREATE UNIQUE INDEX "group_tenant_id_name" ON "groups" ("tenant_id", "name");
-- Create index "groups_identity_id_key" to table: "groups"
CREATE UNIQUE INDEX "groups_identity_id_key" ON "groups" ("identity_id");
