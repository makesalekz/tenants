include:
  - project: 'deploy/dev'
    file: '/go-app-v3.yml'
    ref: 'main'

lint dev:
  extends: .lint-dev

test dev:
  extends: .test-dev

build dev:
  extends: .build-dev

deploy dev:
  extends: .deploy-dev

cleanup:
  extends: .cleanup

release:
  extends: .release

merge:
  extends: .merge

helm-lint:
  extends: .helm-lint  

build:
  extends: .build-shell

migration-build:
  extends: .migration-build

deploy:
  extends: .deploy

rollout:
  extends: .rollout
